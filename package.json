{
  "name": "@communist-prometheus/webring",
  "version": "0.1.0",
  "type": "module",
  "description": "Headless web component for the Revolutionary Internationalists webring — one button, sends the visitor to a random allied site.",
  "license": "AGPL-3.0-or-later",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:e2e": "playwright test",
    "validate:members": "bun scripts/validate-members.ts",
    "typecheck": "tsc --noEmit",
    "build": "bun run validate:members && bun run typecheck && vite build",
    "cdn:build": "bun run build && bun scripts/assemble-cdn.ts",
    "cdn:deploy": "bun run cdn:build && wrangler deploy"
  },
  "devDependencies": {
    "@playwright/test": "^1.58.2",
    "@types/node": "^25.9.1",
    "typescript": "^5.7.0",
    "vite": "^6.0.0",
    "vitest": "^3.0.0"
  }
}
